import { Extraction } from './conversion/extraction';
import { Joining } from './conversion/joining';

export namespace Conversion {
	export const extraction = Extraction;
	export const joining = Joining;
}
